import { Container, Typography } from "@mui/material";
const Result = () => {
  return (
    <Container maxWidth={"sm"}>
      <Typography m={"2rem 0"}>Result</Typography>
    </Container>
  );
};

export default Result;
